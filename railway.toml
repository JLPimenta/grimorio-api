[build]
builder = "nixpacks"
installCommand = "npm ci"
buildCommand = "npm run build"

[deploy]
startCommand = "npm run db:migrate && node dist/src/main"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3