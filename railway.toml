[build]
builder = "nixpacks"

[deploy]
startCommand = "npm run db:migrate && node dist/main"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3